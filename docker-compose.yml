services:
  api:
    build: ./backend
    ports: ["8000:8000"]
    volumes: ["./data:/app/data:ro"]
    environment:
      CORS_ORIGINS: http://localhost:5173
  web:
    build: ./frontend
    ports: ["5173:80"]
    environment:
      VITE_API_URL: http://localhost:8000
